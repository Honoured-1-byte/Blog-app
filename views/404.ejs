<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>404 | Lost in Akasha</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    <!-- Import Orbitron for Futuristic Geometric look -->
    <link
        href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&family=Inter:wght@300;400;600&family=Cinzel:wght@400;700&display=swap"
        rel="stylesheet">

    <style>
        :root {
            /* Astral Void Vars */
            --astral-blue: #2CE8CF;
            --astral-glow: #7FF8FF;
            --mystic-indigo: #6A5ACD;
            --deep-violet: #4B0082;
            --text-white: #EAFBFF;
            --accent-warm: #CC8A52;

            /* Radar Button Vars */
            --deep-blue: #2563eb;
            /* Royal Blue */
            --glow-blue: #60a5fa;
            /* Lighter Blue for glow */
            --void-bg: #020617;
            /* Navy */
        }

        body {
            height: 100vh;
            margin: 0;
            display: flex;
            align-items: center;
            justify-content: flex-start;
            text-align: left;
            padding-left: 10%;
            color: var(--text-white);
            font-family: 'Inter', sans-serif;
            overflow: hidden;

            /* Astral Void Background System */
            background: linear-gradient(rgba(0, 0, 0, 0.55), rgba(0, 0, 0, 0.55)),
                radial-gradient(circle at top right, rgba(26, 28, 41, 0.4), rgba(13, 13, 13, 0.9)),
                url('https://res.cloudinary.com/dnyg7ue5v/image/upload/v1765452880/intersteller_dadgdx.jpg') no-repeat center center/cover;

            backdrop-filter: blur(2px);
        }

        .container {
            position: relative;
            z-index: 2;
        }

        /* --- ASTRAL VOID TYPOGRAPHY --- */

        .error-code {
            font-family: 'Orbitron', sans-serif;
            font-size: 10rem;
            font-weight: 900;
            letter-spacing: 10px;
            line-height: 0.9;
            margin-bottom: 0;

            /* Astral Blue with Glow */
            color: transparent;
            -webkit-text-stroke: 2px var(--astral-blue);
            text-shadow:
                0 0 15px rgba(44, 232, 207, 0.4),
                0 0 30px var(--mystic-indigo);

            position: relative;
        }

        .message {
            font-family: 'Cinzel', serif;
            font-size: 2rem;
            font-weight: 700;
            letter-spacing: 4px;
            color: var(--astral-blue);
            text-shadow: 0 0 7px var(--astral-glow);
            margin-top: 15px;
            margin-bottom: 50px;
            text-transform: uppercase;
        }

        .message::after {
            content: '';
            display: block;
            width: 100px;
            height: 2px;
            background: var(--mystic-indigo);
            margin-top: 15px;
            box-shadow: 0 0 10px var(--mystic-indigo);
        }

        /* --- RADAR BUTTON STYLES --- */
        .btn-radar {
            position: relative;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 15px;

            padding: 18px 50px;
            background: rgba(2, 6, 23, 0.8);
            /* Dark Navy */
            border: 1px solid var(--deep-blue);
            border-radius: 50px;

            color: var(--glow-blue);
            font-size: 1rem;
            font-weight: 700;
            letter-spacing: 2px;
            text-decoration: none;
            text-transform: uppercase;
            transition: all 0.4s ease;
            overflow: hidden;

            /* Add some spacing for the pulse ring to be visible if centered on button, 
               but pulse ring is absolute. */
        }

        /* The "Scanner" Icon */
        .btn-radar i {
            transition: transform 1s ease;
        }

        /* Hover Effects */
        .btn-radar:hover {
            background: var(--deep-blue);
            color: #fff;
            box-shadow: 0 0 40px rgba(37, 99, 235, 0.6);
            border-color: var(--glow-blue);
            transform: translateY(-2px);
        }

        /* Spin the compass on hover */
        .btn-radar:hover i {
            animation: spin 2s infinite linear;
        }

        /* The "Ping" Pulse Animation behind the button */
        .btn-radar::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 100%;
            height: 100%;
            background: var(--deep-blue);
            border-radius: 50px;
            transform: translate(-50%, -50%) scale(0.8);
            opacity: 0;
            z-index: -1;
            transition: all 0.5s;
        }

        /* Pulse Ring - positioned relative to button parent or button? 
           User's code had it in container. I'll put it in button wrapper or use negative margins */
        .radar-wrapper {
            position: relative;
            display: inline-block;
        }

        .pulse-ring {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 200px;
            height: 100px;
            border: 1px solid var(--deep-blue);
            border-radius: 50px;
            animation: radar-ping 3s infinite linear;
            opacity: 0;
            pointer-events: none;
            z-index: -1;
        }

        /* --- DECORATIONS & ANIMATIONS --- */

        .decoration-orb {
            position: absolute;
            width: 8px;
            height: 8px;
            background: var(--accent-warm);
            border-radius: 50%;
            box-shadow: 0 0 10px var(--accent-warm);
            top: -20px;
            left: 10px;
            opacity: 0.8;
            animation: pulse-warm 4s infinite;
        }

        .floating-icon {
            font-size: 4rem;
            color: var(--mystic-indigo);
            opacity: 0.7;
            margin-bottom: 20px;
            animation: float 8s ease-in-out infinite;
            filter: drop-shadow(0 0 10px var(--deep-violet));
        }

        @keyframes float {
            0% {
                transform: translateY(0px);
            }

            50% {
                transform: translateY(-20px);
            }

            100% {
                transform: translateY(0px);
            }
        }

        @keyframes pulse-warm {

            0%,
            100% {
                opacity: 0.5;
                transform: scale(1);
            }

            50% {
                opacity: 1;
                transform: scale(1.5);
            }
        }

        @keyframes spin {
            0% {
                transform: rotate(0deg);
            }

            100% {
                transform: rotate(360deg);
            }
        }

        @keyframes radar-ping {
            0% {
                width: 0px;
                height: 0px;
                opacity: 0.8;
            }

            100% {
                width: 300px;
                height: 150px;
                opacity: 0;
            }
        }
    </style>
</head>

<body>

    <div class="container">
        <!-- Astral Void Icon Layout -->
        <div style="position: relative; display: inline-block;">
            <i class="fa-solid fa-infinity floating-icon"></i>
            <div class="decoration-orb"></div>
        </div>

        <div class="error-code">404</div>

        <p class="message">"The Akasha is too vast to be lost in..."</p>

        <!-- Radar Button Wrapper -->
        <div class="radar-wrapper">
            <div class="pulse-ring"></div>
            <a href="/" class="btn-radar">
                <i class="fa-regular fa-compass fa-lg"></i>
                Return to Origin
            </a>
        </div>
    </div>

</body>

</html>